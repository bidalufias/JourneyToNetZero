node_modules
dist
dist-ssr
build
*.local
.env
.env.*
!.env.example
.DS_Store
*.tsbuildinfo
.vite
.cache

# Playwright/browser scratch
shots/

# Python
__pycache__/
*.pyc

# Generated, but committed: bundle.ts is the deployable unit, so keeping it in
# the repo means the function can be deployed from the Supabase dashboard or
# the Management API without any local tooling. CI checks it stays in sync.
supabase/functions/_src/content.gen.ts
